// TODO : Check if the user is in game (admin or player). If not, redirect to the home page

const GamePage = () => {
    return <p>Hello ! Your current game is : TODO (viewing as a TODO)</p>;
};

export default GamePage;